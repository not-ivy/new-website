{
  "name": "website-static",
  "license": "CC0-1.0",
  "packageManager": "yarn@4.0.0-rc.53",
  "scripts": {
    "build": "rm -rf docs && mkdir -p docs/static && yarn copy-static && concurrently \"npm:build-*\"",
    "build-css": "purgecss --css static/missing.min.css --content index.html --output docs/static",
    "build-html": "html-minifier-terser --collapse-whitespace --remove-comments index.html -o docs/index.html",
    "copy-static": "cp -R static docs/"
  },
  "devDependencies": {
    "concurrently": "^8.2.1",
    "html-minifier-terser": "^7.2.0",
    "purgecss": "^5.0.0"
  }
}
